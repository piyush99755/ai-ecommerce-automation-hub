[
  {
    "id": "column.public.product.lowStockThreshold",
    "label": "Add column \"lowStockThreshold\" to \"product\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "lowStockThreshold",
        "table": "product"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"lowStockThreshold\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "product",
          "lowStockThreshold"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"lowStockThreshold\"",
        "sql": "ALTER TABLE \"public\".\"product\" ADD COLUMN \"lowStockThreshold\" int4 DEFAULT 5 NOT NULL"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"lowStockThreshold\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "product",
          "lowStockThreshold"
        ]
      }
    ]
  }
]